name: Test OOM
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cause OOM
        run: |
          node -e "
            const arr = [];
            while(true) { arr.push(new Array(1000000).fill('x')); }
          "
