name: Test Build Error
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install TypeScript
        run: npm install -g typescript
      - name: Compile
        run: |
          echo "const x: number = 'this is not a number';" > broken.ts
          tsc broken.ts
