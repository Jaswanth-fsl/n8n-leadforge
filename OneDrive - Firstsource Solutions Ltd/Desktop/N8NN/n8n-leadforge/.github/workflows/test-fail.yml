name: Test CI Failure
on:
  push:
    branches:
      - test-ci-fail

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate npm failure
        run: npm install this-package-does-not-exist-xyz-abc
