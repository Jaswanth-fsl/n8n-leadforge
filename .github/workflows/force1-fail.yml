name: Force Fail
on:
  push:
    branches: [main]
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Fail intentionally
        run: |
          echo "ERROR: npm install failed"
          echo "Cannot find module lodash"
          echo "npm ERR! code ENOENT"
          exit 1
